#pragma warning(disable : 4996)
#include <cstdio>
#include <cstring>
typedef long long int ll;
typedef long int l;
const l MAX = 1e9 + 5;
// max possible number of composite numbers
ll n;
l p[MAX];


int main() {
	int q; scanf("%d", &q);
	while (q--) {
		scanf("%lld", &n);
		// if it is a prime, then no splitting.
	}
	return 0;
}