#pragma warning(disable : 4996)
#include <cstdio>
#include <cstring>
const int MAX = 2000;
char s[MAX], t[MAX];
int main() {
	int q; scanf("%d", &q);
	while (q--) {
		scanf("%s", &s); scanf("%s", &t);

	}
	return 0;
}

/*
Can't really get it.
*/